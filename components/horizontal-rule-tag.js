const template = document.createElement('template');
template.innerHTML = `
    <style>
        /* Uncomment and provide a URL if you want to import an external stylesheet */
        /* @import url(); */

        :host {
            /* for the shadow root */
            display: block;
        }

        :host(horizontal-rule-tag) {
            /* if my shadow root is horizontal-rule-tag */
        }

        :host-context(main) {
            /* if one of my ancestors is main */
        }

        ::slotted(*) {
            /* anything that is slotted */
        }

        .horizontal-rule-tag {}
    </style>

    <hr class="horizontal-rule-tag" part="horizontal-rule-tag"></hr>
`;

class HorizontalRuleTag extends HTMLElement {
    constructor() {
        super();

        this.root = this.attachShadow({ mode: 'closed' });
        this.root.innerHTML = template.innerHTML;
    }

    // Define allowed attributes
    static get observedAttributes() {
        return [];
    }

    // Handle values and changes to the attributes
    attributeChangedCallback(_attrName, _oldVal, _newVal) {
        // Handle attribute changes here if needed
    }
}

customElements.define('horizontal-rule-tag', HorizontalRuleTag);

// <horizontal-rule-tag id='some id'>
// </horizontal-rule-tag>
