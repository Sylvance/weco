const template = document.createElement('template');
template.innerHTML = `
    <style>
        /* Uncomment and provide a URL if you want to import an external stylesheet */
        /* @import url(); */

        :host {
            /* for the shadow root */
            display: block;
        }

        :host(keyboard-input-tag) {
            /* if my shadow root is keyboard-input-tag */
        }

        :host-context(main) {
            /* if one of my ancestors is main */
        }

        ::slotted(*) {
            /* anything that is slotted */
        }

        .keyboard-input-tag {}
    </style>

    <kbd class="keyboard-input-tag" part="keyboard-input-tag"></kbd>
`;

class KeyboardInputTag extends HTMLElement {
    constructor() {
        super();

        this.root = this.attachShadow({ mode: 'closed' });
        this.root.innerHTML = template.innerHTML;
    }

    // Define allowed attributes
    static get observedAttributes() {
        return [];
    }

    // Handle values and changes to the attributes
    attributeChangedCallback(_attrName, _oldVal, _newVal) {
        // Handle attribute changes here if needed
    }
}

customElements.define('keyboard-input-tag', KeyboardInputTag);

// <keyboard-input-tag id='some id'>
// </keyboard-input-tag>
